import { setMetadataEnv } from "@plasmicapp/cli";
import { JsOrTs, PlatformOptions, PlatformType, SchemeType } from "./utils/types";
export interface CreatePlasmicAppArgs {
    resolvedProjectPath: string;
    projectId: string;
    platform: PlatformType;
    platformOptions: PlatformOptions;
    scheme: SchemeType;
    jsOrTs: JsOrTs;
    projectApiToken?: string;
    template?: string;
}
export declare function create(args: CreatePlasmicAppArgs): Promise<void>;
export declare function checkValidName(name?: string): boolean;
export declare function checkProjectInput(input: string): boolean;
/** Gets a project ID from an ID itself or a URL. */
export declare function extractProjectId(input: string): string;
export declare function banner(message: string): void;
/**
 * Re-export this so that consumers (e.g. plasmic-action)
 * can use it too
 */
export declare const setMetadata: typeof setMetadataEnv;
